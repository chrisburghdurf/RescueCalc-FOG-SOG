{
  "id": "cribbing-height-planner",
  "toolName": "Cribbing Height Planner (Placeholder)",
  "description": "Estimate layer count and build reminders for crib stacks.",
  "placeholder": true,
  "placeholderLabel": "Placeholder rule. Link this module to actual uploaded manual citations before operational use.",
  "calculator": "cribbingHeight",
  "inputs": [
    { "id": "cribbingThicknessIn", "label": "Cribbing Thickness Actual (in)", "type": "number", "required": true, "min": 2, "max": 12, "step": 0.5, "defaultValue": 4 },
    { "id": "desiredHeightIn", "label": "Desired Height (in)", "type": "number", "required": true, "min": 1, "max": 96, "step": 0.5, "defaultValue": 24 },
    { "id": "baseFootprintFt", "label": "Base Footprint (ft)", "type": "number", "required": true, "min": 1, "max": 12, "step": 0.5, "defaultValue": 4 },
    { "id": "stackCount", "label": "Number of Stacks", "type": "number", "required": true, "min": 1, "max": 12, "step": 1, "defaultValue": 2 }
  ],
  "assumptions": [
    "Stack loading is shared evenly.",
    "Ground and base plates are stable."
  ],
  "warnings": [
    "Remove split or damaged cribbing from service.",
    "Maintain full-contact load path and avoid uneven stacking."
  ],
  "stepTemplates": [
    "Use {layersRequired} layers per stack to reach approximately {estimatedHeightIn} in.",
    "Base footprint is {baseFootprintFt} ft with {stackCount} stack(s).",
    "Pattern reminder: alternate direction each layer and keep centers aligned.",
    "Pause and inspect stack plumb/alignment before loading increases."
  ],
  "citationQueries": [
    { "query": "cribbing lumber dimensions and stacking guidance", "preferredSource": "SOG" },
    { "query": "cribbing safety and height limits", "preferredSource": "FOG" }
  ]
}
