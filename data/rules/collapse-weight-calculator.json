{
  "id": "collapse-weight-calculator",
  "toolName": "Collapse Weight Calculator (Placeholder)",
  "description": "Estimate collapse component weight from material and geometry.",
  "placeholder": true,
  "placeholderLabel": "Placeholder rule. Link this module to actual uploaded manual citations before operational use.",
  "calculator": "collapseWeight",
  "inputs": [
    { "id": "materialType", "label": "Material Type", "type": "select", "required": true, "options": [
      { "value": "reinforced-concrete", "label": "Reinforced Concrete" },
      { "value": "masonry", "label": "Masonry" },
      { "value": "steel", "label": "Steel" },
      { "value": "wood", "label": "Wood" }
    ], "defaultValue": "reinforced-concrete" },
    { "id": "shape", "label": "Shape", "type": "select", "required": true, "options": [
      { "value": "slab", "label": "Slab" },
      { "value": "wall", "label": "Wall" },
      { "value": "beam", "label": "Beam" },
      { "value": "column", "label": "Column" },
      { "value": "rubble", "label": "Rubble Estimate" }
    ], "defaultValue": "slab" },
    { "id": "lengthFt", "label": "Length (ft)", "type": "number", "required": true, "min": 0.1, "max": 200, "step": 0.1, "defaultValue": 10 },
    { "id": "widthFt", "label": "Width (ft)", "type": "number", "required": true, "min": 0.1, "max": 200, "step": 0.1, "defaultValue": 8 },
    { "id": "heightFt", "label": "Height/Thickness (ft)", "type": "number", "required": true, "min": 0.05, "max": 50, "step": 0.05, "defaultValue": 0.5 }
  ],
  "assumptions": [
    "Shape approximates a rectangular prism for volume estimate.",
    "Unit weight uses standard planning values unless document table overrides are linked."
  ],
  "warnings": [
    "Rubble voids, rebar density, moisture, and damage can shift actual weight.",
    "Use engineering confirmation for critical load decisions."
  ],
  "stepTemplates": [
    "Volume estimate: {volumeFt3} ft^3.",
    "Unit weight used: {unitWeightPcf} pcf.",
    "Estimated weight: {estimatedWeightLb} lb.",
    "PSF (if slab/wall context): {estimatedPsf} psf.",
    "Document and verify assumptions before committing lift/shoring plan."
  ],
  "citationQueries": [
    { "query": "unit weight tables concrete masonry steel wood", "preferredSource": "SOG" },
    { "query": "collapse debris weight estimate", "preferredSource": "FOG" }
  ]
}
